<section aria-labelledby="edit-person-heading" class="mx-auto max-w-xl">
  <p class="text-sm font-semibold uppercase tracking-wider text-primary-700 dark:text-primary-400">Shared household</p>
  <h1 id="edit-person-heading" class="mt-2 text-4xl font-bold tracking-tight">Edit <%= @person.name %></h1>

  <%= render "form", person: @person %>
</section>
