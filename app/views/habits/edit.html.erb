<% content_for :title, "Edit #{@habit.name}" %>
<%= render "layouts/activities_navigation" %>
<h1 class="text-3xl font-semibold tracking-tight">Edit <%= @habit.name %></h1>
<p class="mt-3 text-gray-600 dark:text-gray-400">Metric keys, types, and units cannot change after measurement history exists.</p>
<%= render "form", habit: @habit, value_types: @value_types %>
