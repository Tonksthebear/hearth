<nav aria-label="Meals" class="mb-8 max-w-7xl">
  <div class="<%= yass(nav_pills: :list) %>">
    <% @secondary_navigation.each do |item| %>
      <%= link_to item[:label], item[:path],
            aria: (item[:active] ? { current: "page" } : {}),
            data: item[:data],
            class: yass(nav_pills: :tab) %>
    <% end %>
  </div>
</nav>
